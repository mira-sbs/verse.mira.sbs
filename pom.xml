<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <organization>
    <name>may i reflect again?</name>
    <url>https://mira.sbs</url>
  </organization>

  <parent>
    <groupId>sbs.mira</groupId>
    <artifactId>mira</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>verse</artifactId>
  <version>1.0.1</version>
  <packaging>jar</packaging>

  <name>mira-verse</name>
  <description>custom minecraft player competition with soul.</description>
  <url>https://verse.mira.sbs/</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <main.class>sbs.mira.verse.MiraVersePlugin</main.class>
  </properties>

  <build>
    <finalName>${project.name}</finalName>
    <sourceDirectory>${basedir}/src/main/java</sourceDirectory>

    <resources>
      <resource>
        <targetPath>.</targetPath>
        <filtering>true</filtering>
        <directory>${basedir}/src/main/resources</directory>
        <includes>
          <include>config.yml</include>
          <include>plugin.yml</include>
          <include>pvp_messages.yml</include>
        </includes>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.3.2</version>
        <configuration>
          <source>21</source>
          <target>21</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>2.2</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <artifactSet>
                <includes>
                  <include>sbs.mira:core</include>
                  <include>app.ashcon.intake:intake-bukkit:*</include>
                  <include>app.ashcon.intake:intake-core:*</include>
                </includes>
              </artifactSet>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <issueManagement>
    <system>GitHub</system>
    <url>https://github.com/mira-sbs/verse.mira.sbs/issues</url>
  </issueManagement>

  <scm>
    <tag>HEAD</tag>
    <connection>scm:git:git://github.com/mira-sbs/verse.mira.sbs.git</connection>
    <url>https://github.com/mira-sbs/verse.mira.sbs</url>
    <developerConnection>scm:git:ssh://git@github.com:mira-sbs/verse.mira.sbs.git</developerConnection>
  </scm>

  <repositories>
    <repository>
      <id>ashcon.app</id>
      <url>https://repo.ashcon.app/nexus/content/repositories/snapshots/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>sbs.mira</groupId>
      <artifactId>core</artifactId>
      <version>1.0.1</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>app.ashcon.intake</groupId>
      <artifactId>intake-bukkit</artifactId>
      <version>1.3-SNAPSHOT</version>
    </dependency>
  </dependencies>
</project>